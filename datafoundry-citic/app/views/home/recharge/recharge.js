/** * Created by sorcerer on 16/10/8. */angular.module('home.recharge', [        {            files: [                'views/home/recharge/recharge.css'            ]        }    ]).controller('rechargeCtrl', ['market','$scope','$rootScope','account', function (market,$scope,$rootScope,account) {    $scope.firplan=[];    $scope.seaplan=[];    market.get({type:'resources'}, function (data) {        angular.forEach(data.plans, function (plan,i) {            plan.hour = parseInt((plan.price/30/24)*100)/100;            if (plan.region === "铸造一区") {                $scope.firplan.push(plan);            }else {                $scope.seaplan.push(plan);            }            $(".tab_area li").each(function(e){                $(this).click(function(){                    $(this).addClass("active").siblings().removeClass("active");                    $(".list_one:eq("+e+")").show().siblings(".list_one").hide();                })            });        })        //console.log($scope.firplan, $scope.seaplan);        //$scope.plans=$scope.plans.sort(by.open("price"));    })}]);